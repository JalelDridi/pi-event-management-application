/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ReviewsStatisticsComponent } from './reviews-statistics.component';

describe('ReviewsStatisticsComponent', () => {
  let component: ReviewsStatisticsComponent;
  let fixture: ComponentFixture<ReviewsStatisticsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ReviewsStatisticsComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ReviewsStatisticsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/