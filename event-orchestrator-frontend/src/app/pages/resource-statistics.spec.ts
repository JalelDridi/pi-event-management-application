import { ResourceStatistics } from './resource-statistics';

describe('ResourceStatistics', () => {
  it('should create an instance', () => {
    expect(new ResourceStatistics()).toBeTruthy();
  });
});
